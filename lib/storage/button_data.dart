import 'package:hive_ce_flutter/hive_flutter.dart';

class ButtonData extends HiveObject {
  int icon_index = 0;
  bool valid = true; // Stub variable; should be a getter to see if we have a command later;
}
